<UserControl x:Class="CustomWorkBundler.UI.FolderPicker"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:CustomWorkBundler.UI"
             mc:Ignorable="d" 
             d:DesignHeight="400" d:DesignWidth="400">

    <Grid Height="20">
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="auto" />
            <ColumnDefinition Width="*" />
            <ColumnDefinition Width="auto" />
        </Grid.ColumnDefinitions>

        <TextBlock Grid.Column="0" Text="{Binding Label}" Margin="0 0 0 5"/>
        <ComboBox x:Name="Path" Grid.Column="1" Text="{Binding FolderPath, Mode=TwoWay}" IsEditable="True" IsEnabled="{Binding CanEditPath}" Margin="10 0 0 0" ItemsSource="{Binding Suggestions}" />
        <Button Grid.Column="2" Content="Browse" Margin="5 0" Padding="10 0" Click="OnBrowseClick"/>
    </Grid>
</UserControl>
